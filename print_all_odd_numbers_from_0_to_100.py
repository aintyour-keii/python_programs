# Create a variable to hold the current number
# Initialize the current number to 0
current_number = 0

# Using a while loop, iterate over 0 to 100 (inclusive)
while current_number <= 100:
    # Identify if the current number is odd by using the modulo operator (%)
    if current_number % 2 != 0:
    # If the current number is odd, print the number
        print(current_number)
    # Increment count by 1
    current_number += 1
